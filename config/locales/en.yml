# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

en:
  app_name: "Roasted.email"
  phone_numbers:
    show:
      from: From
      message: Message
      time: Time
    index:
      title: Disposable phone numbers
      description: Free service to receive sms messages. Use a phone number form the list below to receive messages from multiple services without revealing your personal phone number.
  mailbox:
    index:
      generate: 🔥 Generate Mailbox 🔥
    inbox:
      generated_at: Mailbox generated at
      emails_received: Emails received
      erases_in: Erases itself in
      generate: Generate another mailbox
      logout: Logout
      delete: Roast mailbox
      last_refresh: Last refresh
      refresh: Refresh
      destroy_confirmation: Are you sure you want to destroy this mailbox? All emails will also be destroyed.
    modals:
      generate_mailbox_help: You can generate as many mailboxes you want. After generating a new one the old one can still be access by using the corresponding URL. Don't forget mailboxes expire and all data is lost.
      session_help: We use cookies and sessions to keep the mailbox persisted on your browser. However, if you don't want to lose access or want to open it elsewhere, copy and save the session url.
    emails:
      emails_list:
        from: FROM
        subject: SUBJECT
        date: DATE
        actions: ACTIONS
  pagy:
    nav:
      next: '>'
      prev: '<'
